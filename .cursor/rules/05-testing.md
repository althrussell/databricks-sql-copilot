# Testing Rules

- Add unit tests for:
  - SQL normalization + fingerprinting
  - candidate scoring
  - prompt building (snapshot tests)
- Add at least one integration test for query execution stub.
- CI should run lint + typecheck + tests.
