# Codex Master Prompt — Databricks SQL Co-Pilot

You are implementing a Databricks App using Next.js App Router + TypeScript + shadcn/ui.
Follow AGENTS.md and .cursor/rules/*.md.

Rules:
- Implement ONLY the requested sprint.
- Keep diffs small and shippable.
- Add loading/empty/error states.
- No raw SQL scattered in components; use /lib/queries.
- Add unit tests for pure logic.

Output requirements:
- After coding, write `docs/output/SPRINT_N_SUMMARY.md` with what changed and how to run.
